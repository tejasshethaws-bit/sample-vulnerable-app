# Dependencies updated to remediate Critical/High CVEs identified by AWS Inspector
Flask==3.0.0       # CVE-2023-30861: session cookie exposure via caching proxy (fixed >=2.3.2)
requests==2.31.0   # CVE-2018-18074: credential disclosure on https-to-http redirect (fixed >=2.20.0)
PyYAML==6.0.1      # CVE-2017-18342, CVE-2020-14343: arbitrary code execution (fixed >=5.4)
boto3==1.7.0       # older boto3 version
